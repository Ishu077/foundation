# ============================================
# Docker Environment Variables Template
# ============================================
# Copy this file to .env.docker and fill in your values
# Command: cp .env.docker.example .env.docker

# ============================================
# DEVELOPMENT ENVIRONMENT
# ============================================
# Use these settings for docker-compose.yml (development)

# Google Gemini AI API Key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=key

# JWT Secret Key (Optional - has default for development)
# For development, you can leave this commented out
JWT_SECRET=dev_jwt_secret_change_in_production

# MongoDB Authentication (Optional - has default for development)
MONGO_URL=local_or_mognodbatlas_one 

# PRODUCTION ENVIRONMENT
# ============================================
# Use these settings for docker-compose.prod.yml (production)
# Uncomment and fill in ALL values below for production deployment

# Google Gemini AI API Key (REQUIRED)
# GEMINI_API_KEY=your_production_gemini_api_key_here

# JWT Secret Key (REQUIRED for production)
# Generate a strong random string:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=your_super_secret_jwt_key_here

# MongoDB Authentication (REQUIRED for production)
# MONGO_ROOT_USERNAME=admin
# MONGO_ROOT_PASSWORD=your_strong_mongodb_password_here

# Redis Authentication (REQUIRED for production)
# REDIS_PASSWORD=your_strong_redis_password_here

# ============================================
# NOTES
# ============================================
# - For development: Only GEMINI_API_KEY is required
# - For production: ALL variables must be set
# - Never commit .env.docker to version control
# - Use strong, unique passwords for production
# - MongoDB and Redis URLs are automatically configured in docker-compose files

