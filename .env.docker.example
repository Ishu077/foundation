# ============================================
# Docker Environment Variables Template
# ============================================
# Copy this file to .env.docker and fill in your values
# Command: cp .env.docker.example .env.docker

# ============================================
# DEVELOPMENT ENVIRONMENT
# ============================================
# Use these settings for docker-compose.yml (development)

# Google Gemini AI API Key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=key

# JWT Secret Key (Optional - has default for development)
# For development, you can leave this commented out
JWT_SECRET=dev_jwt_secret_change_in_production

# MongoDB Authentication (Optional - has default for development)
MONGO_URL=local_or_mognodbatlas_one 

#only use the docker-compose.yml for local development  for the production you can deploy the backend and frontend individually via docker both have their docker in theeir respective directory!


